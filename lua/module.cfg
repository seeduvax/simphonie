MODNAME=lua
MODTYPE=library
USEMOD+=kern sys
TESTUSEMOD+=umdl colibry rest mt
CFLAGS+=
LINKLIB=lua simdeck
INCLUDE_MODS=sol2

LDFLAGS+=

ifneq ($(filter Windows%,$(ARCH)),)
CFLAGS+=-Wa,-mbig-obj
endif
