MODNAME=lua
MODTYPE=library
USEMOD+=kern sys mt
TESTUSEMOD+=umdl colibry rest fmi
CFLAGS+=
LINKLIB=lua simdeck
INCLUDE_MODS=sol2

LDFLAGS+=

ifneq ($(filter Windows%,$(ARCH)),)
CFLAGS+=-Wa,-mbig-obj
endif
